apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: efs-claim
  namespace: elastic-job
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: efs-sc
  resources:
    requests:
      storage: 24Gi
